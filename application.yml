spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://172.17.0.2/sitm_409
    username: postgres
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 9090
tkn:
  api:
    url: localhost:8090/api
    fail-fast: false
    hierarchy: false
  reply:
    rate: 3000
---
spring:
  profiles: via
tkn:
  reply:
    rate: 30000
